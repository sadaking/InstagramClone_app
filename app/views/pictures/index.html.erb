<h2>投稿一覧</h2>
<br>
<table class="PostTable">
  <tr>
    <th>Title</th>
    <th>User</th>
  </tr>
<% @pictures.each do |picture| %>
  <tr>
    <td><%= picture.title %></td>
    <td><%= picture.user.name %></td>
    <td><%= link_to 'SHOW', picture_path(picture.id) %></td>
    <td><%= link_to "EDIT", edit_picture_path(picture.id), data: { confirm: '本当に編集していいですか？' } %></td>
    <td><%= link_to 'DELETE', picture_path(picture.id), method: :delete ,data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>
<br>
<%= link_to '新しくブログを投稿する', new_picture_path %>
